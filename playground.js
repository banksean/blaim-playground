// This is a playground for testing generative code suggestion logging.

// This is hand-written.

function foo() {
  return 'bar';
}

// This is the generative code suggestion.

function fib(n) {
  if (n <= 1) {
    return n;
    } else {
    return fib(n - 1) + fib(n - 2);
  }
}

// And this is more hand-written code.

function bar() {
  return 'foo';
}